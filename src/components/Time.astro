---

---

<script>
/************
Dynamic Data
************/
// define `theClockApp` stored in function to be ran
const theClockApp = () => {
  
  // Get and store current date and time with `new Date()` object
  const timeNow = new Date();
  
  // Get current hours
  let gotHours = timeNow.getHours();
  // Get current minutes
  let gotMinutes = timeNow.getMinutes();
  // Get current seconds
  let gotSeconds = timeNow.getSeconds();
  
  // Check-Check: Is the data correct? 
  // console.log(gotHours, gotMinutes, gotSeconds);
  
  /************
  Format Data
  ************/
  // Get AM or PM
  const gotAmOrPM = gotHours >= 12 ? 'PM' : 'AM';
  // Get 12 hour format
  gotHours = gotHours % 12 || 12;
  // Optionally, append zero to single digit hours
  gotHours = gotHours < 10 ? `${gotHours}` : gotHours;   
  // Optionally, append zero to single digit minutes
  gotMinutes = gotMinutes < 10 ? `0${gotMinutes}` : gotMinutes;
  // Optionally, append zero to single digit seconds
  gotSeconds = gotSeconds < 10 ? `0${gotSeconds}` : gotSeconds;
  
  /************
  Get DOM Elements
  ************/
  // Get Hours
  const hours = document.querySelector(".hours");
  // Get minutes
  const minutes = document.querySelector(".minutes");
  // Get seconds
  const seconds = document.querySelector(".seconds");
  // Get AM or PM
  const amOrPM = document.querySelector('.amOrPM');
  
  /************
  Set DOM Elements
  ************/
  // Set the hours
  hours.textContent = String(gotHours);
  // Set the minutes
  minutes.textContent = String(gotMinutes);
  // Set the seconds
  seconds.textContent = String(gotSeconds);
  // Set AM or PM
  amOrPM.textContent = gotAmOrPM;
    
  }
  
  /************
  Run App
  ************/
  // Re-run `theClockApp` every 1 second (1000 ms)
  setInterval(theClockApp, 1000);
</script>

<section class="time card absolute top-5 left-1/2 text-4xl hover:glass hover:bg-black hover:bg-opacity-30 w-auto px-4 py-2 transform -translate-x-1/2 outlined-text"> <!-- "transform -translate-x-1/2" to position it correctly in the center -->
    <time>
      <data class="hours">HH</data> : <data class="minutes">MM</data> :
      <data class="seconds">SS</data>
      <data class="amOrPM text-2xl">AM/PM</data>
    </time>
<section>



  <style>
    .outlined-text{
      color: white;
      text-shadow: 2px 2px 4px black, 0 0 25px rgba(0,0,0,0.7);
    }
  </style>
